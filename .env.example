# Docker Image Watch - Environment Configuration

# Cron schedule for update checks (crontab format)
# Default: Daily at 4:00 AM
# 
# Format: minute hour day-of-month month day-of-week
#
# Examples:
#   0 4 * * *     - Daily at 4:00 AM
#   0 */6 * * *   - Every 6 hours
#   0 0 * * 0     - Weekly on Sunday at midnight
#   */30 * * * *  - Every 30 minutes
#   0 2 * * 1-5   - Weekdays at 2:00 AM
#
UPDATE_SCHEDULE=0 4 * * *

# Run update check immediately on startup (true/false)
# Set to true to check for updates when the container starts
RUN_ON_STARTUP=false

# =============================================================================
# WEBHOOK CONFIGURATION (optional)
# =============================================================================

# Webhook URL to send reports after each update cycle
# Leave empty to disable webhooks
#
# Supported services (auto-detected):
#   - Discord: https://discord.com/api/webhooks/...
#   - Slack:   https://hooks.slack.com/services/...
#   - Telegram: https://api.telegram.org/bot<TOKEN>/sendMessage?chat_id=<CHAT_ID>
#   - Generic: Any URL accepting POST with JSON
#
WEBHOOK_URL=

# Webhook format (optional, auto-detected from URL by default)
# Options: auto, discord, slack, telegram, markdown, json, generic
WEBHOOK_FORMAT=auto

# When to send webhook notifications
# WEBHOOK_ON_UPDATE: Send when containers are updated (default: true)
# WEBHOOK_ON_ERROR:  Send when errors occur (default: true)  
# WEBHOOK_ALWAYS:    Always send report, even if no changes (default: false)
WEBHOOK_ON_UPDATE=true
WEBHOOK_ON_ERROR=true
WEBHOOK_ALWAYS=false
